<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render "form", commentable: @commentable, comment: @comment %>

<br>

<div>
  <%= link_to t('views.common.show', name: Comment.model_name.human.downcase), polymorphic_url([@commentable, @comment]) %> |
  <%= link_to t('views.detail.back', name: i18n_pluralize(@commentable.model_name.human.downcase)), polymorphic_url([@commentable]) %>
</div>
